import math

class Adder(object):
    def add(self, x, y):
        if x == y:
            return x + y
        else:
            aTest = 5
            anotherTest = aTest - 6
            return y + x
        
    def multiply(self, x, y):
        return x * y

coverage:
  status:
    project:
      default:
        target: 90%
        threshold: null
comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false  
  require_base: no        
  require_head: yes       
  branches: null

codecov:
  ci:
    - dev.azure.com